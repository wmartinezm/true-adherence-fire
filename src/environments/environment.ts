// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyAqWDmjpL2MosubPy7olnrS3NqEodqhWKQ",
    authDomain: "true-adherence-fire.firebaseapp.com",
    projectId: "true-adherence-fire",
    storageBucket: "true-adherence-fire.appspot.com",
    messagingSenderId: "1012815273093",
    appId: "1:1012815273093:web:71fddea96fcffce5e49dec",
    measurementId: "G-6W3WR63RBX"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
